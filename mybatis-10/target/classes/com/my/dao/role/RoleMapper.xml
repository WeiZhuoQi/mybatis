<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my.dao.role.RoleMapper">

    <!--获取角色列表-->
    <select id="getRoleList" resultType="Role">
        select * from smbms_role
    </select>

    <!--添加角色信息-->
    <insert id="add" parameterType="Role">
        insert into smbms_role(roleCode,roleName,createdBy,creationDate)
        values(#{roleCode},#{roleName},#{createdBy},#{creationDate})
    </insert>

    <!--通过Id删除role-->
    <delete id="deleteRoleById" parameterType="_int">
        delete from smbms_role where id = #{id}
    </delete>

    <!--修改角色信息-->
    <update id="modify" parameterType="Role">
        update smbms_role
        <set>
            <if test="roleCode!=null">
                roleCode = #{roleCode},
            </if>
            <if test="roleName!=null">
                roleName = #{roleName},
            </if>
            <if test="modifyBy!=0">
                modifyBy = #{modifyBy},
            </if>
            <if test="modifyDate!=null">
                modifyDate = #{modifyDate}
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过Id获取role-->
    <select id="getRoleById" parameterType="_int" resultType="Role">
        select * from smbms_role where id = #{id}
    </select>

    <!--根据roleCode，进行角色编码的唯一性验证-->
    <select id="roleCodeIsExist" parameterType="String" resultType="_int">
        select count(*) from smbms_role where roleCode = #{roleCode}
    </select>

</mapper>